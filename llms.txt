# azure-functions-scaffold

> Scaffolding CLI for production-ready Azure Functions Python v2 projects.

## Package Info

- PyPI: `pip install azure-functions-scaffold`
- Version: 0.5.0
- Python: >=3.10, <3.15
- License: MIT
- Docs: https://yeongseon.github.io/azure-functions-scaffold/
- Repository: https://github.com/yeongseon/azure-functions-scaffold

## What It Does

CLI tool that generates production-ready Azure Functions Python v2 project scaffolds.
Handles boilerplate setup: `host.json`, `function_app.py`, directory structure, tooling config, and
tests so teams can focus on business logic from the start.

## CLI Commands

```bash
azure-functions-scaffold [GROUP] [COMMAND] [OPTIONS]
afs [GROUP] [COMMAND] [OPTIONS]                    # Short alias
```

### Main Commands

- `afs api new <name>` — Create HTTP API project with strict preset, OpenAPI, validation, and doctor
- `afs api crud <name>` — Create CRUD API project with API features plus database bindings
- `afs api add <name>` — Add HTTP function to an existing project
- `afs worker timer <name>` — Create timer-trigger worker project
- `afs worker queue <name>` — Create queue-trigger worker project
- `afs worker blob <name>` — Create blob-trigger worker project
- `afs worker servicebus <name>` — Create Service Bus-trigger worker project
- `afs worker eventhub <name>` — Create EventHub-trigger worker project
- `afs ai agent <name>` — Create LangGraph AI agent project
- `afs advanced new <name>` — Power-user mode with direct flag control
- `afs advanced add <trigger> <name>` — Add any trigger function to an existing project
- `afs templates` — List available templates
- `afs presets` — List available presets

### Common options (project creation commands)

- `--destination`, `-d` — Base directory where project folder is created
- `--python-version` — Target Python version (`3.10`, `3.11`, `3.12`, `3.13`, `3.14`)
- `--github-actions/--no-github-actions` — Include or skip GitHub Actions workflow
- `--git/--no-git` — Initialize or skip git repository
- `--azd/--no-azd` — Include or skip Azure Developer CLI support files
- `--dry-run` — Preview generation without writing files
- `--overwrite` — Replace existing target directory

### `afs advanced new` additional options

- `--template`, `-t` — Template to render (`http`, `timer`, `queue`, `blob`, `servicebus`, `eventhub`, `cosmosdb`, `durable`, `ai`, `langgraph`)
- `--preset` — Preset to apply (`minimal`, `standard`, `strict`)
- `--with-openapi/--no-openapi` — Include OpenAPI support (HTTP template)
- `--with-validation/--no-validation` — Include request validation support (HTTP template)
- `--with-doctor/--no-doctor` — Include `azure-functions-doctor`
- `--with-db/--no-db` — Include `azure-functions-db` bindings

### Global options

- `--version` — Show installed version
- `-h`, `--help` — Show help

## Templates

| Template | Command | Use Case |
| --- | --- | --- |
| http | `afs api new my-api` | REST APIs, webhooks |
| timer | `afs worker timer my-job` | Scheduled tasks, cron |
| queue | `afs worker queue my-worker` | Message processing (Azurite) |
| blob | `afs worker blob my-blob` | File processing (Azurite) |
| servicebus | `afs worker servicebus my-bus` | Enterprise messaging |
| eventhub | `afs worker eventhub my-stream-worker` | Event stream processing |
| langgraph | `afs ai agent my-agent` | LangGraph AI agent deployment |

## Presets

Presets combine tooling levels and best practices:

- `minimal` — Bare essentials (host.json, function_app.py)
- `standard` — Standard tools (ruff, pytest)
- `strict` — Production-ready checks (ruff, mypy, pytest)

## Quick Start

```bash
# Step 1: Install
pip install azure-functions-scaffold

# Step 2: Create project
afs api new my-api

# Step 3: Install dependencies
cd my-api
pip install -e .

# Step 4: Run locally
func start

# Open http://localhost:7071/api/hello in browser
```

## Optional Features

Intent commands auto-select features based on project goal:

- `afs api new <name>` automatically enables OpenAPI, validation, and doctor
- `afs api crud <name>` automatically enables OpenAPI, validation, doctor, and db
- `afs worker <trigger> <name>` and `afs ai agent <name>` apply trigger-specific defaults

Use `afs advanced new <name>` for explicit feature control:

- `--with-openapi` — Swagger UI + OpenAPI spec endpoints
- `--with-validation` — Pydantic request/response validation
- `--with-doctor` — azure-functions-doctor health checks
- `--with-db` — Database bindings

## Generated Project Layout

```text
my-api/
|- function_app.py          # v2 entrypoint
|- host.json                # Runtime config
|- local.settings.json.example
|- pyproject.toml           # Dependencies
|- app/
|  |- core/
|  |  `- logging.py
|  |- functions/
|  |  `- http.py
|  |- schemas/
|  |  `- request_models.py
|  `- services/
|     `- hello_service.py
`- tests/
   `- test_http.py
```

## Project Requirements

- Project names must start with an alphanumeric character
- Only letters, numbers, hyphens, underscores allowed
- Python 3.10+ required

## Documentation

- [Getting Started](https://yeongseon.github.io/azure-functions-scaffold/getting-started/)
- [CLI Reference](https://yeongseon.github.io/azure-functions-scaffold/cli/)
- [Templates](https://yeongseon.github.io/azure-functions-scaffold/templates/)
- [Architecture](https://yeongseon.github.io/azure-functions-scaffold/architecture/)
- [Deployment Guide](https://yeongseon.github.io/azure-functions-scaffold/deployment/)

## Ecosystem

Part of the Azure Functions Python DX Toolkit:
- azure-functions-validation — Request/response validation
- azure-functions-openapi — OpenAPI spec and Swagger UI
- azure-functions-doctor — Pre-deploy diagnostics CLI
- azure-functions-langgraph — Deploy LangGraph agents as Functions
