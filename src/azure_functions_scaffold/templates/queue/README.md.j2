# {{ project_name }}

Generated by `azure-functions-scaffold`.

Template: `queue`

Preset: `{{ preset_name }}`

Python: `{{ python_version }}`

## Development

```bash
python -m venv .venv
. .venv/bin/activate
pip install -e .{% if include_ruff or include_mypy or include_pytest %}[dev]{% endif %}
{% if include_ruff -%}
ruff check .
ruff format --check .
{% endif -%}
{% if include_mypy -%}
mypy .
{% endif -%}
{% if include_pytest -%}
pytest
{% endif -%}
func start
```

## Local queue development

Start Azurite before running the app locally:

```bash
azurite --silent --location .azurite --debug .azurite/debug.log
```
