# {{ project_name }}

{% if is_preview_python %}
> **Note**: This project targets Python {{ python_version }}, which is **Preview** on Azure Functions today. Flex Consumption remote build and some regions may not yet support it. See the [Microsoft language support matrix](https://learn.microsoft.com/azure/azure-functions/supported-languages) before deploying to production.

{% endif %}

Generated by `azure-functions-scaffold`.

Preset: `{{ preset_name }}`

Python: `{{ python_version }}`
{% if include_openapi %}

OpenAPI: enabled — visit `/api/docs` for Swagger UI
{% endif %}
{% if include_validation %}

Validation: enabled — request/response models validated via `azure-functions-validation`
{% endif %}

## Prerequisites

- Python `{{ python_version }}` or newer
- Azure Functions Core Tools v4 (`func`)
- A local virtual environment (`python -m venv .venv`)
- Optional: Docker for containerized local runs

## Project Structure

```text
.
├── function_app.py
├── app/
│   ├── core/
│   │   ├── config.py
│   │   └── logging.py
│   ├── dependencies/
│   │   └── __init__.py
│   ├── functions/
│   │   ├── health.py
│   │   └── webhooks.py
│   ├── schemas/
│   │   ├── health.py
│   │   └── webhooks.py
│   └── services/
│       ├── health_service.py
│       └── webhook_service.py
└── tests/
    ├── test_health.py
    └── test_webhooks.py
```

## Configuration

- `.funcignore` controls files excluded from local packaging/deployment.
- `host.json` defines Azure Functions host-level runtime configuration.
- `local.settings.json.example` documents required local app settings.
- `app/core/config.py` reads application settings from environment variables.

{% if include_validation %}
## Validation

Request and response payloads are validated with `azure-functions-validation`.
Schemas are defined with `pydantic` models in `app/schemas/webhooks.py`.
Invalid requests return structured 4xx errors before service logic executes.
{% endif %}

{% if include_doctor %}
## Health Check

The health endpoint provides a lightweight runtime health signal.
Use it to verify app startup, dependency wiring, and basic readiness.
It is intended for local checks and platform health probes.
{% endif %}

## Endpoints

| Method | Route | Description |
|--------|-------|-------------|
| GET | `/api/health` | Health check |
| POST | `/api/webhooks/inbound` | Receive inbound webhook event |
{% if include_openapi %}
| GET | `/api/docs` | Swagger UI |
| GET | `/api/openapi.json` | OpenAPI JSON schema |
| GET | `/api/openapi.yaml` | OpenAPI YAML schema |
{% endif %}

## Development

```bash
python -m venv .venv
. .venv/bin/activate
pip install -e .{% if include_ruff or include_mypy or include_pytest %}[dev]{% endif %}
{% if include_ruff -%}
ruff check .
ruff format --check .
{% endif -%}
{% if include_mypy -%}
mypy .
{% endif -%}
{% if include_pytest -%}
pytest
{% endif -%}
func start
```
{% if include_openapi %}

## API Documentation

Once the function app is running, open the following URLs:

- Swagger UI: `http://localhost:7071/api/docs`
- OpenAPI JSON: `http://localhost:7071/api/openapi.json`
- OpenAPI YAML: `http://localhost:7071/api/openapi.yaml`
{% endif %}
