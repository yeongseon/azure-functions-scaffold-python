# {{ project_name }}

{% if is_preview_python %}
> **Note**: This project targets Python {{ python_version }}, which is **Preview** on Azure Functions today. Flex Consumption remote build and some regions may not yet support it. See the [Microsoft language support matrix](https://learn.microsoft.com/azure/azure-functions/supported-languages) before deploying to production.

{% endif %}

Generated by `azure-functions-scaffold`.

Preset: `{{ preset_name }}`

Python: `{{ python_version }}`

Template: `langgraph` — deploys a LangGraph agent on Azure Functions.

## Development

```bash
python -m venv .venv
. .venv/bin/activate
pip install -e .{% if include_ruff or include_mypy or include_pytest %}[dev]{% endif %}
{% if include_ruff -%}
ruff check .
ruff format --check .
{% endif -%}
{% if include_mypy -%}
mypy .
{% endif -%}
{% if include_pytest -%}
pytest
{% endif -%}
func start
```

## Agent Endpoints

Once the function app is running, the following endpoints are available:

- Invoke agent: `POST http://localhost:7071/api/echo_agent/invoke`
- Stream agent: `POST http://localhost:7071/api/echo_agent/stream`
- Health check: `GET http://localhost:7071/api/echo_agent/health`
