# Azure Functions Scaffold

`azure-functions-scaffold` is a scaffolding CLI for Azure Functions Python v2 projects.

It focuses on a small number of goals:

- create a clean project layout quickly
- keep generated output testable and lint-clean
- stay aligned with the Azure Functions Python v2 programming model

See the CLI guide for commands, the template spec for generated output, and the architecture page
for repository design boundaries.
