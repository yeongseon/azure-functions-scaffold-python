# Contributing

See `CONTRIBUTING.md` in the repository root for contributor guidance.

Use the Makefile as the canonical entry point for local workflows:

- `make install`
- `make check-all`
- `make docs`
